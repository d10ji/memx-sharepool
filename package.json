{
  "name": "memex",
  "version": "1.0.0",
  "private": true,
  "description": "A personal index that auto-lists the standalone HTML pages in this folder.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "@vercel/edge": "^1.1.2"
  }
}
